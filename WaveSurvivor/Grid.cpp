#include "Grid.h"

template <typename T>
Grid<T>* Grid<T>::BuildGrid(const std::vector<T>& objects)
{
	// Divide map into cells based on its size
	// iterate over the objects and add it to the cell it is in
}

template<typename T>
bool Grid<T>::CheckCollision(Rectangle a, Rectangle b)
{
	return false;
}
